import { InfiniteScollerDirective } from './infinite-scoller.directive';

describe('InfiniteScollerDirective', () => {
  it('should create an instance', () => {
    const directive = new InfiniteScollerDirective();
    expect(directive).toBeTruthy();
  });
});
